#cloud-config
autoinstall:
  version: 1
  manage_etc_hosts: true
  locale: en_US.UTF-8
  keyboard:
    layout: us
  refresh_installer:
    update: false
  ssh:
    install-server: true
    allow-pw: true
    disable_root: true
    ssh_quiet_keygen: true
    allow_public_ssh_keys: true
  packages:
    - qemu-guest-agent
    - sudo
  storage:
    layout:
      name: direct
    swap:
      size: 0
  user-data:
    package_upgrade: true
    timezone: America/Chicago
    users:
      - name: ol3d
        groups: [adm, sudo]
        lock-passwd: true
        sudo: ALL=(ALL) NOPASSWD:ALL
        shell: /bin/bash
        # SSH public keys should be added here
        # Generate hash with: mkpasswd -m sha-512
        ssh_authorized_keys:
          # Add your SSH public key here during provisioning
          - ssh-rsa PLACEHOLDER_KEY_REPLACE_BEFORE_BUILD
