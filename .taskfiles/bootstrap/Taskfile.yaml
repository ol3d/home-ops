---
version: "3"

vars:
  ANSIBLE_DIR: "{{.ROOT_DIR}}/lab/provision/ansible"

tasks:
  workspace:
    desc: Bootstrap local workspace
    dir: "{{.ANSIBLE_DIR}}"
    cmds:
      - ansible-galaxy install -r requirements.yml --force
      - ansible-galaxy collection install -r requirements.yml --force
